let db;let maxAnimePerPage=50;let maxStaffPerPage=25;const excludedFormats={music:true,manga:true,novel:true,one_shot:true};const currentRequestTimestamp=(new Date).getTime();let minimizeTransaction=false;let currentYear;let onlyGetNewEntries;let retryCount;let willProcessList;self.onmessage=async({data})=>{if(!db)await IDBinit();if(data?.hasOwnProperty("minimizeTransaction")){minimizeTransaction=data?.minimizeTransaction}else{currentYear=(new Date).getFullYear();onlyGetNewEntries=data?.onlyGetNewEntries??false;retryCount=0;let animeEntries=await retrieveJSON("animeEntries")||{};let excludedEntries=await retrieveJSON("excludedEntries")||{};willProcessList=false;if(isJsonObject(animeEntries)&&jsonIsEmpty(animeEntries)){self.postMessage({getEntries:true})}else{getNewEntries(animeEntries,excludedEntries)}}async function getNewEntries(animeEntries,excludedEntries){self.postMessage({progress:0});let currentAnimeIDs=Object.keys(animeEntries).concat(Object.keys(excludedEntries)).join(",");let entriesCount=0;self.postMessage({status:"Checking New Entries"});let shouldProcessList;function recallGNE(page){self.postMessage({progress:25});fetch("https://graphql.anilist.co",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json","Cache-Control":"max-age=31536000, immutable"},body:JSON.stringify({query:`{Page(page:${page},perPage:${maxAnimePerPage}){pageInfo{hasNextPage}media(type:ANIME,id_not_in:[${currentAnimeIDs}]){id updatedAt title{romaji english userPreferred}relations{edges{relationType node{id popularity}}}description siteUrl averageScore episodes duration trending popularity favourites format genres status coverImage{large}trailer{id thumbnail site}bannerImage tags{name rank category}studios{nodes{name siteUrl isAnimationStudio}}startDate{month day year}endDate{month day year}seasonYear season staff(perPage:${maxStaffPerPage},page:1,sort:[RELEVANCE]){edges{node{name{userPreferred}siteUrl}role}}nextAiringEpisode{episode airingAt}}}}`})}).then(async response=>{let headers=response.headers;let result=await response.json();return{result:result,headers:headers}}).then(async({result,headers})=>{let error;if(typeof(error=result?.errors?.[0]?.message)==="string"){if(onlyGetNewEntries){self.postMessage({errorDuringInit:true})}++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:(error?error+" ":"")+`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallGNE(page)},6e4)}else{let Page=result?.data?.Page;let media=Page?.media||[];let hasUpdatedEntry=false;let hasExcludedEntry=false;let newAddedEntries=0;if(media instanceof Array&&media.length){media.forEach(anime=>{if(typeof anime?.id==="number"){if((typeof anime?.format!=="string"||!excludedFormats[anime?.format?.trim?.()?.toLowerCase?.()])&&!anime?.genres?.some?.(genre=>genre?.trim?.()?.toLowerCase?.()==="hentai")){if(anime?.genres instanceof Array){let unique={};anime.genres=anime.genres.filter(genre=>{if(genre&&!unique[genre]){unique[genre]=true;return true}else{return false}})}if(anime?.tags instanceof Array){let unique={};anime.tags=anime.tags.filter(tag=>{let _tag=tag?.name;if(_tag&&!unique[_tag]){unique[_tag]=true;return true}else{return false}})}if(anime?.studios?.nodes instanceof Array){let unique={};anime.studios.nodes=anime.studios.nodes.filter(studio=>{let _studio=studio?.name;if(_studio&&!unique[_studio]){unique[_studio]=true;return true}else{return false}})}if(excludedEntries?.hasOwnProperty?.(anime.id)){delete excludedEntries[anime.id];hasExcludedEntry=true}else if(!animeEntries?.hasOwnProperty?.(anime.id)){anime.dateAdded=currentRequestTimestamp}else if(animeEntries?.[anime.id]?.dateAdded){anime.dateAdded=animeEntries[anime.id].dateAdded}animeEntries[anime.id]=anime;hasUpdatedEntry=true;newAddedEntries++}else{if(animeEntries?.hasOwnProperty?.(anime.id)){newAddedEntries--;delete animeEntries[anime.id];hasUpdatedEntry=true}excludedEntries[anime.id]=1;hasExcludedEntry=true}}})}retryCount=0;let hasNextPage=Page?.pageInfo?.hasNextPage??true;if(hasNextPage&&media.length>0){shouldProcessList=shouldProcessList||hasUpdatedEntry;if(!minimizeTransaction){if(hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}await saveJSON(animeEntries,"animeEntries")}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}}entriesCount=entriesCount+Math.max(0,newAddedEntries);if(entriesCount>0){self.postMessage({status:`${entriesCount} anime has been added`})}if(headers?.get("x-ratelimit-remaining")>0){return recallGNE(++page)}else{if(onlyGetNewEntries){self.postMessage({errorDuringInit:true})}let secondsPassed=60;let rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3);setTimeout(()=>{clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallGNE(++page)},6e4)}}else{self.postMessage({progress:100});if(shouldProcessList||hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}await saveJSON(animeEntries,"animeEntries");self.postMessage({updateRecommendationList:true});shouldProcessList=false}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}self.postMessage({status:null});if(onlyGetNewEntries){self.postMessage({done:true})}else{updateAiringAnime(animeEntries,excludedEntries)}}}}).catch(error=>{if(!navigator.onLine){self.postMessage({status:"Currently Offline"});self.postMessage({message:"Currently Offline"});return}let headers=error.headers;let errorText=error.message;if(errorText==="User not found"){self.postMessage({status:null});self.postMessage({message:"User not found"})}else{if(headers?.get("x-ratelimit-remaining")>0){return recallGNE(page)}else{if(onlyGetNewEntries){self.postMessage({errorDuringInit:true})}++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallGNE(page)},6e4)}}})}recallGNE(1)}async function updateAiringAnime(animeEntries,excludedEntries){self.postMessage({progress:0});let airingAnime=Object.values(animeEntries).filter(({seasonYear,status})=>{return ncsCompare(status,"releasing")||ncsCompare(status,"not_yet_released")&&parseInt(seasonYear)>=currentYear});let airingAnimeIDs=airingAnime.map(({id})=>id);let pastAiringEpisodeIDs=airingAnime.filter(anime=>{if(isJsonObject(anime?.nextAiringEpisode)){let releaseDate=new Date(anime?.nextAiringEpisode?.airingAt*1e3);if(releaseDate instanceof Date&&!isNaN(releaseDate)&&releaseDate<=(new Date).getTime()){return true}else{return false}}else{return false}}).map(({id})=>id);let animeLength=airingAnimeIDs.length+pastAiringEpisodeIDs.length;let currentNonProcessedLength=animeLength;let airingAnimeIDsString=airingAnimeIDs.join(",");let pastAiringEpisodeIDsString=pastAiringEpisodeIDs.join(",");self.postMessage({status:"Checking Latest Entries"});let lastAnimeUpdate=await retrieveJSON("lastAnimeUpdate")||new Date(1670770349*1e3);let lastAiringUpdateDate=await retrieveJSON("lastAiringAnimeUpdate")||lastAnimeUpdate;let latestAiringUpdateAtDate;let shouldProcessList;function recallUAA(page){fetch("https://graphql.anilist.co",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json","Cache-Control":"max-age=31536000, immutable"},body:JSON.stringify({query:`{Page(page:${page},perPage:${maxAnimePerPage}){pageInfo{hasNextPage}media(type:ANIME,id_in:[${airingAnimeIDsString||""}],sort:[UPDATED_AT_DESC]){id updatedAt title{romaji english userPreferred}relations{edges{relationType node{id popularity}}}description siteUrl averageScore episodes duration trending popularity favourites format genres status coverImage{large}trailer{id thumbnail site}bannerImage tags{name rank category}studios{nodes{name siteUrl isAnimationStudio}}startDate{month day year}endDate{month day year}seasonYear season staff(perPage:${maxStaffPerPage},page:1,sort:[RELEVANCE]){pageInfo{hasNextPage}edges{node{name{userPreferred}siteUrl}role}}nextAiringEpisode{episode airingAt}}}}`})}).then(async response=>{let headers=response.headers;let result=await response.json();return{result:result,headers:headers}}).then(async({result,headers})=>{let error;if(typeof(error=result?.errors?.[0]?.message)==="string"){++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:(error?error+" ":"")+`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUAA(page)},6e4)}else{let Page=result?.data?.Page;let media=Page?.media||[];let currentAnimeUpdateDate;let hasUpdatedEntry=false;let hasExcludedEntry=false;if(media instanceof Array){for(let anime of media){if(typeof anime?.id==="number"){currentAnimeUpdateDate=new Date(anime.updatedAt*1e3);if(currentAnimeUpdateDate instanceof Date&&!isNaN(currentAnimeUpdateDate)){if(latestAiringUpdateAtDate===undefined||currentAnimeUpdateDate>latestAiringUpdateAtDate){latestAiringUpdateAtDate=currentAnimeUpdateDate}if(lastAiringUpdateDate>=currentAnimeUpdateDate&&lastAiringUpdateDate instanceof Date&&!isNaN(lastAiringUpdateDate)){break}}airingAnimeIDs=airingAnimeIDs.filter(_id=>_id!==anime.id);if((typeof anime?.format!=="string"||!excludedFormats[anime?.format?.trim?.()?.toLowerCase?.()])&&!anime?.genres?.some?.(genre=>genre?.trim?.()?.toLowerCase?.()==="hentai")){if(anime?.genres instanceof Array){let unique={};anime.genres=anime.genres.filter(genre=>{if(genre&&!unique[genre]){unique[genre]=true;return true}else{return false}})}if(anime?.tags instanceof Array){let unique={};anime.tags=anime.tags.filter(tag=>{let _tag=tag?.name;if(_tag&&!unique[_tag]){unique[_tag]=true;return true}else{return false}})}if(anime?.studios?.nodes instanceof Array){let unique={};anime.studios.nodes=anime.studios.nodes.filter(studio=>{let _studio=studio?.name;if(_studio&&!unique[_studio]){unique[_studio]=true;return true}else{return false}})}if(excludedEntries?.hasOwnProperty?.(anime.id)){delete excludedEntries[anime.id];hasExcludedEntry=true}else if(!animeEntries?.hasOwnProperty?.(anime.id)){anime.dateAdded=currentRequestTimestamp}else if(animeEntries?.[anime.id]?.dateAdded){anime.dateAdded=animeEntries[anime.id].dateAdded}animeEntries[anime.id]=anime;hasUpdatedEntry=true}else{if(animeEntries?.hasOwnProperty?.(anime.id)){delete animeEntries[anime.id];hasUpdatedEntry=true}excludedEntries[anime.id]=1;hasExcludedEntry=true}}}}retryCount=0;let hasNextPage=Page?.pageInfo?.hasNextPage??true;if(hasNextPage&&media.length>0&&(lastAiringUpdateDate<currentAnimeUpdateDate||!(currentAnimeUpdateDate instanceof Date)||isNaN(currentAnimeUpdateDate)||!(lastAiringUpdateDate instanceof Date)||isNaN(lastAiringUpdateDate))){if(currentNonProcessedLength>airingAnimeIDs.length+pastAiringEpisodeIDs.length){currentNonProcessedLength=airingAnimeIDs.length+pastAiringEpisodeIDs.length;let processedLength=Math.max(animeLength-(airingAnimeIDs.length+pastAiringEpisodeIDs.length),0);let percentage=100*(processedLength/animeLength);percentage=percentage>=0?percentage:0;self.postMessage({progress:percentage});self.postMessage({status:`${percentage.toFixed(2)}% Updating Entries`})}shouldProcessList=shouldProcessList||hasUpdatedEntry;if(!minimizeTransaction){if(hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}await saveJSON(animeEntries,"animeEntries")}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}}if(headers?.get("x-ratelimit-remaining")>0){return recallUAA(++page)}else{let secondsPassed=60;let rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3);setTimeout(()=>{clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUAA(++page)},6e4)}}else{if(shouldProcessList||hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}await saveJSON(animeEntries,"animeEntries");self.postMessage({updateRecommendationList:true});shouldProcessList=false}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}if((shouldProcessList||hasUpdatedEntry)&&latestAiringUpdateAtDate instanceof Date&&!isNaN(latestAiringUpdateAtDate)){lastAiringUpdateDate=latestAiringUpdateAtDate;await saveJSON(lastAiringUpdateDate,"lastAiringUpdateDate")}if(pastAiringEpisodeIDs.length>0){currentNonProcessedLength=pastAiringEpisodeIDs.length;self.postMessage({status:null});recallUNAE(1)}else{self.postMessage({progress:100});self.postMessage({status:"100% Updating Entries"});let lastRunnedAutoUpdateDate=new Date;await saveJSON(lastRunnedAutoUpdateDate,"lastRunnedAutoUpdateDate");self.postMessage({lastRunnedAutoUpdateDate:lastRunnedAutoUpdateDate});self.postMessage({status:null});updateNonRecentEntries(animeEntries,excludedEntries,lastAnimeUpdate)}}}}).catch(error=>{if(!navigator.onLine){self.postMessage({status:"Currently Offline"});self.postMessage({message:"Currently Offline"});return}let headers=error.headers;let errorText=error.message;if(errorText==="User not found"){self.postMessage({status:null});self.postMessage({message:"User not found"})}else{if(headers?.get("x-ratelimit-remaining")>0){return recallUAA(page)}else{++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUAA(page)},6e4)}}console.error(error)})}function recallUNAE(page){fetch("https://graphql.anilist.co",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json","Cache-Control":"max-age=31536000, immutable"},body:JSON.stringify({query:`{Page(page:${page},perPage:${maxAnimePerPage}){pageInfo{hasNextPage}media(type:ANIME,id_in:[${pastAiringEpisodeIDsString||""}]){id nextAiringEpisode{episode airingAt}}}}`})}).then(async response=>{let headers=response.headers;let result=await response.json();return{result:result,headers:headers}}).then(async({result,headers})=>{let error;if(typeof(error=result?.errors?.[0]?.message)==="string"){++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:(error?error+" ":"")+`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUNAE(page)},6e4)}else{let Page=result?.data?.Page;let media=Page?.media||[];let hasUpdatedEntry=false;let hasExcludedEntry=false;if(media instanceof Array){for(let anime of media){if(typeof anime?.id==="number"){pastAiringEpisodeIDs=pastAiringEpisodeIDs.filter(_id=>_id!==anime.id);if((typeof anime?.format!=="string"||!excludedFormats[anime?.format?.trim?.()?.toLowerCase?.()])&&!anime?.genres?.some?.(genre=>genre?.trim?.()?.toLowerCase?.()==="hentai")){if(excludedEntries?.hasOwnProperty?.(anime.id)){delete excludedEntries[anime.id];hasExcludedEntry=true}if(isJsonObject(animeEntries?.[anime.id])){animeEntries[anime.id].nextAiringEpisode=anime?.nextAiringEpisode;hasUpdatedEntry=true}}else{if(animeEntries?.hasOwnProperty?.(anime.id)){delete animeEntries[anime.id];hasUpdatedEntry=true}excludedEntries[anime.id]=1;hasExcludedEntry=true}}}}retryCount=0;let hasNextPage=Page?.pageInfo?.hasNextPage??true;if(hasNextPage&&media.length>0){if(currentNonProcessedLength>pastAiringEpisodeIDs.length){currentNonProcessedLength=pastAiringEpisodeIDs.length;let processedLength=Math.max(animeLength-pastAiringEpisodeIDs.length,0);let percentage=100*(processedLength/animeLength);percentage=percentage>=0?percentage:0;self.postMessage({progress:percentage});self.postMessage({status:`${percentage.toFixed(2)}% Updating Entries`})}shouldProcessList=shouldProcessList||hasUpdatedEntry;if(!minimizeTransaction){if(hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}await saveJSON(animeEntries,"animeEntries")}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}}if(headers?.get("x-ratelimit-remaining")>0){return recallUNAE(++page)}else{let secondsPassed=60;let rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3);setTimeout(()=>{clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUNAE(++page)},6e4)}}else{self.postMessage({progress:100});if(shouldProcessList||hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}self.postMessage({status:"100% Updating Entries"});await saveJSON(animeEntries,"animeEntries");self.postMessage({updateRecommendationList:true});shouldProcessList=false}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}let lastRunnedAutoUpdateDate=new Date;await saveJSON(lastRunnedAutoUpdateDate,"lastRunnedAutoUpdateDate");self.postMessage({lastRunnedAutoUpdateDate:lastRunnedAutoUpdateDate});self.postMessage({status:null});updateNonRecentEntries(animeEntries,excludedEntries,lastAnimeUpdate)}}}).catch(error=>{if(!navigator.onLine){self.postMessage({status:"Currently Offline"});self.postMessage({message:"Currently Offline"});return}let headers=error.headers;let errorText=error.message;if(errorText==="User not found"){self.postMessage({status:null});self.postMessage({message:"User not found"})}else{if(headers?.get("x-ratelimit-remaining")>0){return recallUNAE(page)}else{++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUNAE(page)},6e4)}}console.error(error)})}recallUAA(1)}async function updateNonRecentEntries(animeEntries,excludedEntries,lastAnimeUpdate){let recursingUpdatedAtDate;let latestUpdateAtDate;let lastUpdateAtDate=lastAnimeUpdate||new Date(1670770349*1e3);let largestDif=-Infinity;let excludedAnimeIds=Object.keys(excludedEntries).join(",");self.postMessage({status:"Checking Additional Entries"});self.postMessage({progress:0});function recallGOUD(){self.postMessage({progress:25});fetch("https://graphql.anilist.co",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json","Cache-Control":"max-age=31536000, immutable"},body:JSON.stringify({query:`{Page(page:1,perPage:1){media(type:ANIME,id_not_in:[${excludedAnimeIds}],sort:[UPDATED_AT]){updatedAt}}}`})}).then(async response=>{let headers=response.headers;let result=await response.json();return{result:result,headers:headers}}).then(async({result,headers})=>{let error;if(typeof(error=result?.errors?.[0]?.message)==="string"){++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:(error?error+" ":"")+`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallGOUD()},6e4)}else{self.postMessage({progress:100});let currentOldestUpdateAtDate=new Date((result?.data?.Page?.media?.[0]?.updatedAt||1670770349)*1e3);if(currentOldestUpdateAtDate instanceof Date&&!isNaN(currentOldestUpdateAtDate)){if(currentOldestUpdateAtDate.getTime()>lastUpdateAtDate.getTime()){lastUpdateAtDate=currentOldestUpdateAtDate}}self.postMessage({progress:0});recallUNRE(1)}}).catch(()=>{if(!navigator.onLine){self.postMessage({status:"Currently Offline"});self.postMessage({message:"Currently Offline"});return}if(headers?.get("x-ratelimit-remaining")>0){return recallGOUD()}else{++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallGOUD()},6e4)}})}let shouldProcessList;let percentage=0;function recallUNRE(page){fetch("https://graphql.anilist.co",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json","Cache-Control":"max-age=31536000, immutable"},body:JSON.stringify({query:`{Page(page:${page},perPage:${maxAnimePerPage}){pageInfo{hasNextPage}media(type:ANIME,id_not_in:[${excludedAnimeIds}],sort:[UPDATED_AT_DESC]){id updatedAt title{romaji english userPreferred}relations{edges{relationType node{id popularity}}}description siteUrl averageScore episodes duration trending popularity favourites format genres status coverImage{large}trailer{id thumbnail site}bannerImage tags{name rank category}studios{nodes{name siteUrl isAnimationStudio}}startDate{month day year}endDate{month day year}seasonYear season staff(perPage:${maxStaffPerPage},page:1,sort:[RELEVANCE]){pageInfo{hasNextPage}edges{node{name{userPreferred}siteUrl}role}}nextAiringEpisode{episode airingAt}}}}`})}).then(async response=>{let headers=response.headers;let result=await response.json();return{result:result,headers:headers}}).then(async({result,headers})=>{let error;if(typeof(error=result?.errors?.[0]?.message)==="string"){++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:(error?error+" ":"")+`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUNRE(page)},6e4)}else{let Page=result?.data?.Page;let media=Page?.media||[];let hasUpdatedEntry=false;let hasExcludedEntry=false;if(media instanceof Array){for(let anime of media){if(typeof anime?.id==="number"){if(typeof anime.updatedAt==="number"){let currentAnimeUpdateDate=new Date(anime.updatedAt*1e3);if(currentAnimeUpdateDate instanceof Date&&!isNaN(currentAnimeUpdateDate)){if(latestUpdateAtDate===undefined||currentAnimeUpdateDate>latestUpdateAtDate){latestUpdateAtDate=currentAnimeUpdateDate;largestDif=Math.max(largestDif,latestUpdateAtDate.getTime()-lastUpdateAtDate.getTime())}if(recursingUpdatedAtDate instanceof Date&&!isNaN(recursingUpdatedAtDate)&&currentAnimeUpdateDate<recursingUpdatedAtDate){recursingUpdatedAtDate=currentAnimeUpdateDate;if(lastAnimeUpdate>=recursingUpdatedAtDate&&recursingUpdatedAtDate instanceof Date&&!isNaN(recursingUpdatedAtDate)&&lastAnimeUpdate instanceof Date&&!isNaN(lastAnimeUpdate)){break}else if(latestUpdateAtDate instanceof Date&&!isNaN(latestUpdateAtDate)&&lastUpdateAtDate<=recursingUpdatedAtDate&&latestUpdateAtDate>recursingUpdatedAtDate){let lastUpdateAtDateTime=lastUpdateAtDate.getTime();let recursingUpdatedAtDateTime=recursingUpdatedAtDate.getTime();percentage=(largestDif-(recursingUpdatedAtDateTime-lastUpdateAtDateTime))/largestDif*100}}else if(recursingUpdatedAtDate===undefined){recursingUpdatedAtDate=currentAnimeUpdateDate;if(lastAnimeUpdate>=recursingUpdatedAtDate&&recursingUpdatedAtDate instanceof Date&&!isNaN(recursingUpdatedAtDate)&&lastAnimeUpdate instanceof Date&&!isNaN(lastAnimeUpdate)){break}else if(latestUpdateAtDate instanceof Date&&!isNaN(latestUpdateAtDate)&&lastUpdateAtDate<=recursingUpdatedAtDate&&latestUpdateAtDate>recursingUpdatedAtDate){let lastUpdateAtDateTime=lastUpdateAtDate.getTime();let recursingUpdatedAtDateTime=recursingUpdatedAtDate.getTime();percentage=(largestDif-(recursingUpdatedAtDateTime-lastUpdateAtDateTime))/largestDif*100}}}}if((typeof anime?.format!=="string"||!excludedFormats[anime?.format?.trim?.()?.toLowerCase?.()])&&!anime?.genres?.some?.(genre=>genre?.trim?.()?.toLowerCase?.()==="hentai")){if(anime?.genres instanceof Array){let unique={};anime.genres=anime.genres.filter(genre=>{if(genre&&!unique[genre]){unique[genre]=true;return true}else{return false}})}if(anime?.tags instanceof Array){let unique={};anime.tags=anime.tags.filter(tag=>{let _tag=tag?.name;if(_tag&&!unique[_tag]){unique[_tag]=true;return true}else{return false}})}if(anime?.studios?.nodes instanceof Array){let unique={};anime.studios.nodes=anime.studios.nodes.filter(studio=>{let _studio=studio?.name;if(_studio&&!unique[_studio]){unique[_studio]=true;return true}else{return false}})}if(excludedEntries?.hasOwnProperty?.(anime.id)){delete excludedEntries[anime.id];hasExcludedEntry=true}else if(!animeEntries?.hasOwnProperty?.(anime.id)){anime.dateAdded=currentRequestTimestamp}else if(animeEntries?.[anime.id]?.dateAdded){anime.dateAdded=animeEntries[anime.id].dateAdded}animeEntries[anime.id]=anime;hasUpdatedEntry=true}else{if(animeEntries?.hasOwnProperty?.(anime.id)){delete animeEntries[anime.id];hasUpdatedEntry=true}excludedEntries[anime.id]=1;hasExcludedEntry=true}}}}let hasNextPage=Page?.pageInfo?.hasNextPage??true;if(hasNextPage&&media.length>0&&(lastAnimeUpdate<recursingUpdatedAtDate||!(recursingUpdatedAtDate instanceof Date)||isNaN(recursingUpdatedAtDate)||!(lastAnimeUpdate instanceof Date)||isNaN(lastAnimeUpdate))){if(percentage>=.01){self.postMessage({status:`${percentage.toFixed(2)}% Updating Additional Entries`})}else{self.postMessage({status:"Updating Additional Entries"})}self.postMessage({progress:percentage});shouldProcessList=shouldProcessList||hasExcludedEntry;if(!minimizeTransaction){if(hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}await saveJSON(animeEntries,"animeEntries")}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}}if(headers?.get("x-ratelimit-remaining")>0){return recallUNRE(++page)}else{let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:(error?error+" ":"")+`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3);setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUNRE(++page)},6e4)}}else{self.postMessage({progress:100});if(shouldProcessList||hasUpdatedEntry){if(!willProcessList){willProcessList=true;await saveJSON(true,"shouldProcessRecommendation")}self.postMessage({status:"100% Updating Additional Entries"});await saveJSON(animeEntries,"animeEntries");self.postMessage({updateRecommendationList:true})}if(hasExcludedEntry){await saveJSON(excludedEntries,"excludedEntries")}if((shouldProcessList||hasUpdatedEntry)&&latestUpdateAtDate instanceof Date&&!isNaN(latestUpdateAtDate)){lastAnimeUpdate=latestUpdateAtDate;await saveJSON(lastAnimeUpdate,"lastAnimeUpdate")}self.postMessage({status:null});self.postMessage({done:true})}}}).catch(error=>{if(!navigator.onLine){self.postMessage({status:"Currently Offline"});self.postMessage({message:"Currently Offline"});return}let headers=error.headers;let errorText=error.message;if(errorText==="User not found"){self.postMessage({status:null});self.postMessage({message:"User not found"})}else{if(headers?.get("x-ratelimit-remaining")>0){return recallUNRE(page)}else{++retryCount;if(retryCount>=2){self.postMessage({status:"Request Timeout"})}let rateLimitInterval;if(retryCount<2){let secondsPassed=60;rateLimitInterval=setInterval(()=>{self.postMessage({progress:(60-secondsPassed)/60*100});self.postMessage({status:`Rate Limit: ${msToTime(secondsPassed*1e3)}`});--secondsPassed},1e3)}setTimeout(()=>{if(rateLimitInterval)clearInterval(rateLimitInterval);self.postMessage({progress:100});self.postMessage({status:"Retrying"});return recallUNRE(page)},6e4)}}})}recallGOUD()}};async function IDBinit(){return await new Promise(resolve=>{request=indexedDB.open("Kanshi.Anime.Recommendations.Anilist.W~uPtWCq=vG$TR:Zl^#t<vdS]I~N70",1);request.onerror=error=>{console.error(error)};request.onsuccess=event=>{db=event.target.result;return resolve()};request.onupgradeneeded=event=>{db=event.target.result;db.createObjectStore("MyObjectStore");let transaction=event.target.transaction;transaction.oncomplete=()=>{return resolve()}}})}async function saveJSON(data,name){return await new Promise(async(resolve,reject)=>{try{let write=db.transaction("MyObjectStore","readwrite").objectStore("MyObjectStore").openCursor();write.onsuccess=async event=>{let put=await db.transaction("MyObjectStore","readwrite").objectStore("MyObjectStore").put(data,name);put.onsuccess=event=>{return resolve()};put.onerror=event=>{return resolve()}};write.onerror=async error=>{console.error(error);return reject()}}catch(ex){console.error(ex)}})}async function retrieveJSON(name){return await new Promise(resolve=>{try{let read=db.transaction("MyObjectStore","readwrite").objectStore("MyObjectStore").get(name);read.onsuccess=()=>{return resolve(read.result)};read.onerror=error=>{console.error(error);return resolve()}}catch(ex){console.error(ex);return resolve()}})}function msToTime(duration,limit){try{if(duration<1e3){return"0s"}let seconds=Math.floor(duration/1e3%60),minutes=Math.floor(duration/6e4%60),hours=Math.floor(duration/36e5%24),days=Math.floor(duration/864e5%7),weeks=Math.floor(duration/6048e5%4),months=Math.floor(duration/24192e5%12),years=Math.floor(duration/290304e5%10),decades=Math.floor(duration/290304e6%10),century=Math.floor(duration/290304e7%10),millenium=Math.floor(duration/290304e8%10);let time=[];if(millenium>0)time.push(`${millenium}mil`);if(century>0)time.push(`${century}cen`);if(decades>0)time.push(`${decades}dec`);if(years>0)time.push(`${years}y`);if(months>0)time.push(`${months}mon`);if(weeks>0)time.push(`${weeks}w`);if(days>0)time.push(`${days}d`);if(hours>0)time.push(`${hours}h`);if(minutes>0)time.push(`${minutes}m`);if(seconds>0)time.push(`${seconds}s`);if(limit>0){time=time.slice(0,limit)}return time.join(" ")||"0s"}catch(e){return""}}function isJsonObject(obj){return Object.prototype.toString.call(obj)==="[object Object]"}function jsonIsEmpty(obj){for(const key in obj){return false}return true}function ncsCompare(str1,str2){if(typeof str1!=="string"||typeof str2!=="string"){return false}return str1.toLowerCase()===str2.toLowerCase()}